{% extends 'base_admin.html.twig' %}

{% block title %}Modifier Ticket - Administration{% endblock %}

{% block page_title %}Modifier un ticket{% endblock %}

{% block body %}
    <div style="max-width: 800px; margin: 0 auto;">
        <h2 style="margin-bottom: 20px;">Modifier le ticket</h2>

        {{ include('ticket/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div style="margin-top: 30px;">
            <button type="submit" class="btn btn-primary" form="ticket_form">Mettre à jour</button>
            <a href="{{ path('app_ticket_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>

        <div style="margin-top: 40px;">
            {{ include('ticket/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}